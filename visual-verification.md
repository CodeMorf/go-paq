# Verificación visual — 24 de agosto de 2026

La captura desktop de `/` muestra el logo GoPaq, el halcón, navegación en español y una tarjeta hero sin métricas numéricas simuladas. La tarjeta usa descripciones honestas: visibilidad según registros disponibles, GPS autorizado y trazabilidad auditable.

La captura desktop de `/docs-api` muestra documentación en español, CTA de login y un único contrato visible de cotización. Las rutas `/admin`, `/sucursal`, `/driver` y `/cliente` sin sesión muestran el login reservado con avatar del halcón, copy en español y botón de autenticación segura. No se pudo verificar el estado autenticado ni los formularios operativos sin iniciar sesión; no se inventan resultados de ese flujo.

Pendiente de esta ronda: capturas mobile de landing, docs y login; revisión autenticada requiere una sesión válida del usuario.

## Verificación responsive móvil

A 390×844, la landing conserva el CTA principal, el CTA de tracking y los tres beneficios sin cortes; el header reduce el texto de marca y mantiene el botón de iniciar envío accesible. `/docs-api` reorganiza el header, conserva el CTA de login y el índice API sin desbordamiento crítico en la captura superior. `/admin` presenta una tarjeta de login compacta, centrada y legible; el botón ocupa el ancho disponible y el avatar/logo permanece visible. No se validó un portal autenticado por ausencia de sesión de prueba.
