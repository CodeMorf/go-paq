import React, { FormEvent, useEffect, useMemo, useState } from 'react';
import { Link, useLocation } from 'react-router-dom';
import { ArrowRight, Check, Clock3, Globe2, MapPin, Menu, PackageCheck, Phone, ShieldCheck, Truck, X } from 'lucide-react';
import { ApiClient } from '../../api/client';
import { GoPaqLogo } from '../ui/GoPaqLogo';

type PublicBranch = { id: string; code: string; name: string; city: string; address: string; phone?: string; is_hub?: number; latitude?: number; longitude?: number };

const services = [
  { slug: 'envios', title: 'Envíos locales y nacionales', description: 'Recogemos, transportamos y entregamos con trazabilidad de principio a fin.', icon: PackageCheck, accent: 'from-orange-500 to-amber-400' },
  { slug: 'courier-internacional', title: 'Courier internacional', description: 'Tu casillero, recepción y entrega final en un solo flujo.', icon: Globe2, accent: 'from-sky-500 to-cyan-400' },
  { slug: 'mudanzas', title: 'Mudanzas', description: 'Planificación, inventario, equipo y evidencia para mover lo que importa.', icon: Truck, accent: 'from-violet-500 to-fuchsia-400' },
  { slug: 'carga-pesada', title: 'Carga pesada', description: 'Pallets, equipos y mercancía especial con despacho coordinado.', icon: ShieldCheck, accent: 'from-emerald-500 to-lime-400' },
  { slug: 'ultima-milla', title: 'Última milla', description: 'Despacho inteligente para que cada entrega llegue con claridad y control.', icon: Clock3, accent: 'from-rose-500 to-orange-400' }
];

const navLinks = [
  ['Servicios', '/servicios'],
  ['Rastreo', '/rastreo'],
  ['Cotizar', '/cotizar'],
  ['Sucursales', '/sucursales'],
  ['Nosotros', '/nosotros'],
  ['Contacto', '/contacto']
] as const;

function usePublicPath() {
  const { pathname } = useLocation();
  return pathname.replace(/\/$/, '') || '/';
}

export const PublicSite: React.FC = () => {
  const path = usePublicPath();
  const [menuOpen, setMenuOpen] = useState(false);
  const isHome = path === '/';

  useEffect(() => { setMenuOpen(false); }, [path]);

  return <div className="min-h-screen overflow-x-hidden bg-[#f7f8fa] text-slate-900">
    <header className="sticky top-0 z-40 border-b border-slate-200/70 bg-white/90 backdrop-blur-xl">
      <div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 lg:px-8">
        <Link to="/" aria-label="GoPaq inicio"><GoPaqLogo variant="horizontal" size="sm" showSlogan={false} theme="light" /></Link>
        <nav className="hidden items-center gap-7 lg:flex" aria-label="Navegación principal">
          {navLinks.map(([label, href]) => <Link key={href} to={href} className={`text-sm font-semibold transition hover:text-orange-600 ${path === href || (href === '/servicios' && path.startsWith('/servicios/')) ? 'text-orange-600' : 'text-slate-600'}`}>{label}</Link>)}
        </nav>
        <div className="hidden items-center gap-3 lg:flex"><Link to="/login" className="rounded-xl px-4 py-2.5 text-sm font-bold text-slate-700 hover:bg-slate-100">Iniciar sesión</Link><Link to="/cotizar" className="rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-bold text-white shadow-lg shadow-slate-900/10 transition hover:bg-orange-600">Cotiza ahora <ArrowRight className="ml-1 inline h-4 w-4" /></Link></div>
        <button type="button" className="rounded-xl p-2 text-slate-700 lg:hidden" onClick={() => setMenuOpen(v => !v)} aria-label={menuOpen ? 'Cerrar menú' : 'Abrir menú'} aria-expanded={menuOpen}>{menuOpen ? <X /> : <Menu />}</button>
      </div>
      {menuOpen && <nav className="border-t border-slate-200 bg-white px-5 py-4 lg:hidden" aria-label="Navegación móvil"><div className="mx-auto max-w-7xl space-y-1">{navLinks.map(([label, href]) => <Link key={href} to={href} className="block rounded-xl px-3 py-3 text-sm font-bold text-slate-700 hover:bg-orange-50 hover:text-orange-600">{label}</Link>)}<div className="mt-3 grid grid-cols-2 gap-2 border-t border-slate-100 pt-3"><Link to="/login" className="rounded-xl border border-slate-200 px-3 py-3 text-center text-sm font-bold">Entrar</Link><Link to="/cotizar" className="rounded-xl bg-slate-950 px-3 py-3 text-center text-sm font-bold text-white">Cotizar</Link></div></div></nav>}
    </header>

    {isHome ? <Home /> : path === '/servicios' ? <Services /> : path.startsWith('/servicios/') ? <ServiceDetail slug={path.split('/')[2] || 'envios'} /> : path === '/rastreo' ? <TrackingPage /> : path === '/cotizar' ? <QuotePage /> : path === '/sucursales' ? <BranchesPage /> : path === '/nosotros' ? <AboutPage /> : <ContactPage />}
    <footer className="border-t border-slate-200 bg-slate-950 text-slate-300"><div className="mx-auto grid max-w-7xl gap-10 px-5 py-12 md:grid-cols-[1.5fr_1fr_1fr_1fr] lg:px-8"><div><GoPaqLogo variant="horizontal" size="sm" showSlogan={false} theme="dark" /><p className="mt-5 max-w-xs text-sm leading-6 text-slate-400">Logística clara para mover paquetes, negocios y oportunidades.</p></div><FooterColumn title="Explora" links={navLinks.slice(0, 3)} /><FooterColumn title="Servicios" links={services.slice(0, 4).map(service => [service.title, `/servicios/${service.slug}`] as const)} /><div><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-400">Atención</p><a href="mailto:hola@gopaq.lat" className="mt-4 block text-sm hover:text-white">hola@gopaq.lat</a><p className="mt-2 text-sm text-slate-500">Canal telefónico: NO CONFIGURADO</p></div></div><div className="mx-auto max-w-7xl border-t border-slate-800 px-5 py-5 text-xs text-slate-500 lg:px-8">© {new Date().getFullYear()} GoPaq. Todos los derechos reservados.</div></footer>
  </div>;
};

const FooterColumn: React.FC<{ title: string; links: ReadonlyArray<readonly [string, string]> }> = ({ title, links }) => <div><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-400">{title}</p><div className="mt-4 space-y-3">{links.map(([label, href]) => <Link key={href} to={href} className="block text-sm text-slate-400 hover:text-white">{label}</Link>)}</div></div>;

const Home: React.FC = () => <main>
  <section className="relative isolate overflow-hidden bg-slate-950"><div className="absolute inset-0 -z-10 bg-[radial-gradient(circle_at_78%_10%,rgba(249,115,22,0.28),transparent_28%),radial-gradient(circle_at_20%_70%,rgba(14,165,233,0.15),transparent_30%)]" /><div className="mx-auto grid max-w-7xl items-center gap-14 px-5 py-20 md:py-28 lg:grid-cols-[1.05fr_.95fr] lg:px-8"><div><p className="mb-5 inline-flex items-center gap-2 rounded-full border border-orange-400/30 bg-orange-400/10 px-3 py-1.5 text-xs font-black uppercase tracking-[0.16em] text-orange-300"><span className="h-2 w-2 rounded-full bg-orange-400" />Logística que se mueve contigo</p><h1 className="max-w-3xl text-5xl font-black leading-[1.03] tracking-tight text-white md:text-7xl">Cada envío tiene un destino. <span className="text-orange-400">Nosotros lo hacemos llegar.</span></h1><p className="mt-6 max-w-xl text-lg leading-8 text-slate-300">Una operación logística más simple para tus envíos, tu negocio y tus clientes. Cotiza, rastrea y coordina desde un mismo lugar.</p><div className="mt-9 flex flex-col gap-3 sm:flex-row"><Link to="/cotizar" className="rounded-2xl bg-orange-500 px-6 py-4 text-center text-sm font-black text-white shadow-xl shadow-orange-500/20 transition hover:bg-orange-400">Cotizar un envío <ArrowRight className="ml-2 inline h-4 w-4" /></Link><Link to="/rastreo" className="rounded-2xl border border-slate-700 px-6 py-4 text-center text-sm font-black text-white transition hover:border-orange-400 hover:text-orange-300">Rastrear una guía</Link></div><div className="mt-10 flex flex-wrap gap-5 text-xs font-semibold text-slate-400"><span><Check className="mr-1 inline h-4 w-4 text-emerald-400" />Trazabilidad real</span><span><Check className="mr-1 inline h-4 w-4 text-emerald-400" />Cobertura nacional</span><span><Check className="mr-1 inline h-4 w-4 text-emerald-400" />Atención humana</span></div></div><div className="relative mx-auto flex min-h-[31rem] w-full max-w-xl items-center justify-center sm:min-h-[36rem]"><div className="absolute inset-x-10 top-8 bottom-10 rounded-[3rem] bg-gradient-to-br from-orange-500/25 via-sky-500/10 to-violet-500/15 blur-3xl" /><div className="absolute right-0 top-8 z-20 hidden max-w-[12rem] rounded-2xl border border-white/10 bg-white/[0.08] p-4 text-white backdrop-blur md:block"><p className="text-[10px] font-black uppercase tracking-[0.2em] text-orange-300">Equipo GoPaq</p><p className="mt-2 text-sm font-black leading-5">Cada paso con seguimiento.</p></div><img src="/assets/brand/gopaq-mascot.png" alt="Mascota mensajera de GoPaq" width="986" height="1502" loading="eager" decoding="async" className="relative z-10 h-[29rem] w-auto object-contain drop-shadow-2xl sm:h-[36rem]" /><div className="absolute bottom-2 left-0 z-20 max-w-[15rem] rounded-2xl border border-white/10 bg-white/[0.94] p-4 text-slate-950 shadow-xl"><p className="text-[10px] font-black uppercase tracking-[0.2em] text-orange-600">Identidad GoPaq</p><p className="mt-2 text-sm font-black leading-5">Logística clara, cercana y confiable.</p></div></div></div></section>
  <section className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="max-w-2xl"><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-600">Lo que hacemos</p><h2 className="mt-3 text-4xl font-black tracking-tight text-slate-950 md:text-5xl">Una solución para cada forma de mover.</h2><p className="mt-4 text-lg leading-8 text-slate-600">Desde un paquete hasta una operación completa, GoPaq conecta personas, sucursales y conductores con información útil.</p></div><div className="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{services.map(service => <ServiceCard key={service.slug} {...service} />)}</div></section>
  <section className="bg-white"><div className="mx-auto grid max-w-7xl gap-12 px-5 py-20 lg:grid-cols-2 lg:px-8"><div><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-600">Hecho para operar</p><h2 className="mt-3 text-4xl font-black tracking-tight md:text-5xl">Menos incertidumbre. Más entregas bajo control.</h2></div><div className="grid gap-6 sm:grid-cols-2"><Feature icon={ShieldCheck} title="Información confiable" text="Cada actualización nace de una operación registrada en GoPaq." /><Feature icon={MapPin} title="Visibilidad de punta a punta" text="Consulta el estado de tus envíos y el avance de tu operación." /><Feature icon={Clock3} title="Ritmo de negocio" text="Cotiza y coordina con procesos claros para responder más rápido." /><Feature icon={Phone} title="Acompañamiento" text="Cuando necesites ayuda, nuestro equipo está cerca." /></div></div></section>
  <section className="bg-orange-500"><div className="mx-auto flex max-w-7xl flex-col items-start justify-between gap-7 px-5 py-14 md:flex-row md:items-center lg:px-8"><div><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-100">Empieza hoy</p><h2 className="mt-2 text-3xl font-black text-white md:text-4xl">Mueve tu próxima entrega con GoPaq.</h2></div><Link to="/cotizar" className="rounded-2xl bg-white px-6 py-4 text-sm font-black text-orange-600 shadow-xl transition hover:bg-orange-50">Ver mi cotización <ArrowRight className="ml-2 inline h-4 w-4" /></Link></div></section>
</main>;

const StatusRow: React.FC<{ label: string; detail: string; tone: string }> = ({ label, detail, tone }) => <div className="flex items-center gap-3"><span className={`h-3 w-3 rounded-full ${tone}`} /><div className="flex-1"><p className="text-sm font-black">{label}</p><p className="text-xs text-slate-500">{detail}</p></div><Check className="h-4 w-4 text-emerald-500" /></div>;
const Feature: React.FC<{ icon: React.ElementType; title: string; text: string }> = ({ icon: Icon, title, text }) => <div><Icon className="h-6 w-6 text-orange-500" /><h3 className="mt-3 font-black">{title}</h3><p className="mt-1 text-sm leading-6 text-slate-500">{text}</p></div>;
const ServiceCard: React.FC<{ slug: string; title: string; description: string; icon: React.ElementType; accent: string }> = ({ slug, title, description, icon: Icon, accent }) => <Link to={`/servicios/${slug}`} className="group rounded-3xl border border-slate-200 bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:border-orange-200 hover:shadow-xl"><div className={`flex h-12 w-12 items-center justify-center rounded-2xl bg-gradient-to-br ${accent} text-white shadow-lg`}><Icon /></div><h3 className="mt-6 text-xl font-black">{title}</h3><p className="mt-3 text-sm leading-6 text-slate-500">{description}</p><span className="mt-6 inline-flex items-center text-sm font-black text-orange-600">Conoce más <ArrowRight className="ml-2 h-4 w-4 transition group-hover:translate-x-1" /></span></Link>;

const Services: React.FC = () => <PageHero eyebrow="Servicios" title="La logística que tu operación necesita." intro="Elige el servicio, comparte los datos y deja que GoPaq conecte el siguiente paso." content={<div className="grid gap-10 lg:grid-cols-[minmax(0,1fr)_19rem] lg:items-start"><div className="grid gap-5 md:grid-cols-2">{services.map(service => <ServiceCard key={service.slug} {...service} />)}</div><aside className="overflow-hidden rounded-[2rem] border border-slate-200 bg-white p-6 shadow-sm"><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-600">Equipo GoPaq</p><h2 className="mt-3 text-2xl font-black leading-tight">Personas que hacen que cada entrega cuente.</h2><p className="mt-3 text-sm leading-6 text-slate-500">La tecnología organiza el camino; nuestro equipo cuida cada paso de la operación.</p><div className="relative mt-6 flex h-[24rem] items-end justify-center overflow-hidden rounded-2xl bg-gradient-to-b from-sky-50 to-white"><img src="/assets/brand/gopaq-operator.png" alt="Operadora logística de GoPaq" width="931" height="1510" loading="lazy" decoding="async" className="h-full w-auto max-w-none object-contain object-bottom" /></div></aside></div>} />;

const ServiceDetail: React.FC<{ slug: string }> = ({ slug }) => {
  const service = services.find(item => item.slug === slug) || services[0];
  const Icon = service.icon;
  return <PageHero eyebrow="Servicio GoPaq" title={service.title} intro={service.description} content={<div className="grid gap-8 lg:grid-cols-[1fr_.8fr]"><div className="rounded-3xl border border-slate-200 bg-white p-7"><div className={`flex h-14 w-14 items-center justify-center rounded-2xl bg-gradient-to-br ${service.accent} text-white`}><Icon /></div><h2 className="mt-7 text-2xl font-black">Un flujo claro para cada entrega</h2><div className="mt-6 space-y-4"><FlowStep number="01" text="Comparte los datos de origen, destino y servicio." /><FlowStep number="02" text="Recibe una cotización calculada por GoPaq." /><FlowStep number="03" text="Crea tu envío y consulta su trazabilidad." /></div><Link to="/cotizar" className="mt-8 inline-block rounded-xl bg-slate-950 px-5 py-3 text-sm font-black text-white">Cotizar este servicio <ArrowRight className="ml-2 inline h-4 w-4" /></Link></div><div className="rounded-3xl bg-slate-950 p-7 text-white"><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-400">Diseñado para crecer</p><h2 className="mt-4 text-3xl font-black">Un mismo lugar para coordinar.</h2><p className="mt-4 leading-7 text-slate-300">Tus clientes, sucursales y conductores pueden trabajar con información alineada y estados que se actualizan desde la operación real.</p><ul className="mt-7 space-y-4 text-sm text-slate-300"><li><Check className="mr-2 inline h-4 w-4 text-emerald-400" />Seguimiento centralizado</li><li><Check className="mr-2 inline h-4 w-4 text-emerald-400" />Roles y accesos por área</li><li><Check className="mr-2 inline h-4 w-4 text-emerald-400" />Historial operativo</li></ul></div></div>} />;
};

const FlowStep: React.FC<{ number: string; text: string }> = ({ number, text }) => <div className="flex gap-4"><span className="font-mono text-sm font-black text-orange-500">{number}</span><p className="text-sm leading-6 text-slate-600">{text}</p></div>;

const TrackingPage: React.FC = () => { const [tracking, setTracking] = useState(''); const [result, setResult] = useState<any>(null); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const submit = async (event: FormEvent) => { event.preventDefault(); setLoading(true); setError(''); setResult(null); const response = await ApiClient.getTracking(tracking.trim()); setLoading(false); if (!response.success) setError(response.error || 'No encontramos esa guía.'); else setResult(response.shipment); }; return <PageHero eyebrow="Rastreo" title="Sigue tu envío sin perder el hilo." intro="Escribe tu número de guía y consulta la línea de tiempo registrada por GoPaq." content={<div className="mx-auto max-w-2xl rounded-3xl border border-slate-200 bg-white p-6 shadow-xl sm:p-8"><form onSubmit={submit} className="flex flex-col gap-3 sm:flex-row"><label htmlFor="public-tracking" className="sr-only">Número de guía</label><input id="public-tracking" value={tracking} onChange={event => setTracking(event.target.value)} placeholder="GP-..." className="min-w-0 flex-1 rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none focus:border-orange-500 focus:ring-4 focus:ring-orange-500/10" required /><button className="rounded-xl bg-orange-500 px-5 py-3 text-sm font-black text-white hover:bg-orange-600" disabled={loading}>{loading ? 'Consultando…' : 'Rastrear guía'}</button></form>{error && <p role="alert" className="mt-4 rounded-xl border border-rose-200 bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}{result && <div className="mt-7 border-t border-slate-100 pt-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-600">{result.trackingNumber}</p><h2 className="mt-1 text-2xl font-black">{statusLabel(result.status)}</h2></div><span className="rounded-full bg-orange-50 px-3 py-1 text-xs font-black text-orange-700">{result.serviceType}</span></div><div className="mt-6 space-y-4">{(result.events || []).map((event: any) => <div key={event.id} className="flex gap-3"><span className="mt-1.5 h-2.5 w-2.5 rounded-full bg-orange-500" /><div><p className="text-sm font-black">{event.description}</p><p className="text-xs text-slate-500">{event.location || 'GoPaq'} · {formatDate(event.created_at)}</p></div></div>)}</div></div>}</div>} />; };

const QuotePage: React.FC = () => { const [form, setForm] = useState({ serviceType: 'local', originCity: 'Santo Domingo', destCity: 'Santo Domingo', weightKg: '1', lengthCm: '20', widthCm: '15', heightCm: '10' }); const [quote, setQuote] = useState<any>(null); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const update = (key: string, value: string) => setForm(prev => ({ ...prev, [key]: value })); const submit = async (event: FormEvent) => { event.preventDefault(); setLoading(true); setError(''); setQuote(null); const response = await ApiClient.calculateQuote({ ...form, weightKg: Number(form.weightKg), lengthCm: Number(form.lengthCm), widthCm: Number(form.widthCm), heightCm: Number(form.heightCm) }); setLoading(false); if (!response.success) setError(response.error || 'No se pudo calcular la cotización.'); else setQuote(response.quote); }; return <PageHero eyebrow="Cotizador" title="Conoce el costo antes de mover." intro="La cotización se calcula en el backend con el tipo de servicio y las dimensiones de tu paquete." content={<div className="grid gap-8 lg:grid-cols-[1fr_.8fr]"><form onSubmit={submit} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-xl sm:p-8"><div className="grid gap-4 sm:grid-cols-2"><Field label="Servicio"><select value={form.serviceType} onChange={event => update('serviceType', event.target.value)} className={fieldClass}><option value="local">Envío local</option><option value="express">Express</option><option value="nacional">Nacional</option><option value="internacional">Internacional</option><option value="mudanza">Mudanza</option><option value="carga_pesada">Carga pesada</option></select></Field><Field label="Peso (kg)"><input type="number" min="0.1" step="0.1" value={form.weightKg} onChange={event => update('weightKg', event.target.value)} className={fieldClass} required /></Field><Field label="Ciudad de origen"><input value={form.originCity} onChange={event => update('originCity', event.target.value)} className={fieldClass} required /></Field><Field label="Ciudad de destino"><input value={form.destCity} onChange={event => update('destCity', event.target.value)} className={fieldClass} required /></Field><Field label="Largo (cm)"><input type="number" min="1" value={form.lengthCm} onChange={event => update('lengthCm', event.target.value)} className={fieldClass} required /></Field><Field label="Ancho (cm)"><input type="number" min="1" value={form.widthCm} onChange={event => update('widthCm', event.target.value)} className={fieldClass} required /></Field><Field label="Alto (cm)"><input type="number" min="1" value={form.heightCm} onChange={event => update('heightCm', event.target.value)} className={fieldClass} required /></Field></div>{error && <p role="alert" className="mt-4 rounded-xl border border-rose-200 bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<button disabled={loading} className="mt-6 w-full rounded-xl bg-slate-950 px-5 py-3.5 text-sm font-black text-white hover:bg-orange-600">{loading ? 'Calculando…' : 'Calcular cotización'}</button></form><div className="rounded-3xl bg-slate-950 p-7 text-white">{quote ? <><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-400">Cotización calculada</p><p className="mt-5 text-5xl font-black">{money(quote.total, quote.currency)}</p><div className="mt-7 space-y-3 border-t border-slate-800 pt-5 text-sm text-slate-300"><p className="flex justify-between"><span>Servicio</span><b className="text-white">{quote.serviceType}</b></p><p className="flex justify-between"><span>Peso facturable</span><b className="text-white">{quote.billableWeightKg} kg</b></p><p className="flex justify-between"><span>Referencia</span><b className="text-white">{quote.quoteReference || 'Calculada'}</b></p></div><Link to="/register" className="mt-8 block rounded-xl bg-orange-500 px-5 py-3 text-center text-sm font-black text-white hover:bg-orange-400">Crear cuenta y continuar</Link></> : <><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-400">Precio claro</p><h2 className="mt-5 text-3xl font-black">Sin sorpresas en el siguiente paso.</h2><p className="mt-4 leading-7 text-slate-300">Completa los datos para obtener una estimación calculada por el motor de tarifas de GoPaq.</p></>}</div></div>} />; };

const BranchesPage: React.FC = () => { const [branches, setBranches] = useState<PublicBranch[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { ApiClient.getPublicBranches().then(result => { if (result.success) setBranches(result.branches); setLoading(false); }); }, []); return <PageHero eyebrow="Sucursales" title="Estamos más cerca de tu operación." intro="Consulta nuestras ubicaciones y encuentra el punto GoPaq que te conviene." content={loading ? <LoadingCard /> : branches.length === 0 ? <EmptyCard text="No hay sucursales publicadas en este momento." /> : <div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{branches.map(branch => <div key={branch.id} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><div className="rounded-xl bg-orange-50 p-3 text-orange-500"><MapPin /></div>{branch.is_hub ? <span className="rounded-full bg-slate-950 px-2.5 py-1 text-[10px] font-black uppercase tracking-wider text-white">Hub</span> : null}</div><h2 className="mt-5 text-xl font-black">{branch.name}</h2><p className="mt-2 text-sm text-slate-500">{branch.address}, {branch.city}</p>{branch.phone && <a className="mt-4 block text-sm font-bold text-orange-600" href={`tel:${branch.phone}`}><Phone className="mr-2 inline h-4 w-4" />{branch.phone}</a>}</div>)}</div>} />; };

const AboutPage: React.FC = () => <PageHero eyebrow="Nosotros" title="Movemos con intención." intro="GoPaq nace para hacer que la logística sea más visible, más ordenada y más humana." content={<div className="grid overflow-hidden rounded-3xl bg-slate-950 text-white md:grid-cols-[1fr_.8fr]"><div className="p-8 md:p-12"><p className="max-w-3xl text-2xl font-black leading-tight md:text-4xl">La tecnología ayuda. La confianza se construye con cada entrega registrada, cada paquete cuidado y cada respuesta a tiempo.</p><div className="mt-10 grid gap-6 border-t border-slate-800 pt-8 sm:grid-cols-3"><Stat value="Cotiza" label="antes de mover" /><Stat value="Rastrea" label="cada actualización" /><Stat value="Coordina" label="tu operación" /></div></div><div className="flex items-center justify-center border-t border-slate-800/80 bg-slate-900/60 p-8 md:border-l md:border-t-0 md:p-10"><img src="/assets/brand/gopaq-logo-lockup.png" alt="GoPaq, logística puerta a puerta" width="1063" height="522" loading="lazy" decoding="async" className="w-full max-w-[28rem]" /></div></div>} />;
const ContactPage: React.FC = () => <PageHero eyebrow="Contacto" title="Hablemos de lo que necesitas mover." intro="Si quieres coordinar una operación o conocer nuestros servicios, escríbenos." content={<div className="grid gap-6 md:grid-cols-2"><a href="mailto:hola@gopaq.lat" className="rounded-3xl border border-slate-200 bg-white p-7 shadow-sm transition hover:border-orange-300"><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-600">Correo</p><p className="mt-4 text-2xl font-black">hola@gopaq.lat</p><p className="mt-2 text-sm text-slate-500">Cuéntanos sobre tu envío u operación.</p></a><div className="rounded-3xl border border-amber-200 bg-amber-50 p-7 shadow-sm"><p className="text-xs font-black uppercase tracking-[0.2em] text-amber-700">Teléfono</p><p className="mt-4 text-2xl font-black text-amber-950">NO CONFIGURADO</p><p className="mt-2 text-sm text-amber-800">El canal telefónico se publicará cuando exista un número operativo confirmado.</p></div></div>} />;

const PageHero: React.FC<{ eyebrow: string; title: string; intro: string; content: React.ReactNode }> = ({ eyebrow, title, intro, content }) => <main><section className="bg-slate-950"><div className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><p className="text-xs font-black uppercase tracking-[0.2em] text-orange-400">{eyebrow}</p><h1 className="mt-4 max-w-4xl text-5xl font-black leading-tight tracking-tight text-white md:text-6xl">{title}</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-slate-300">{intro}</p></div></section><section className="mx-auto max-w-7xl px-5 py-12 lg:px-8">{content}</section></main>;
const Field: React.FC<{ label: string; children: React.ReactNode }> = ({ label, children }) => <label className="block"><span className="mb-1.5 block text-xs font-black uppercase tracking-wider text-slate-500">{label}</span>{children}</label>;
const fieldClass = 'w-full rounded-xl border border-slate-200 bg-slate-50 px-3.5 py-3 text-sm outline-none focus:border-orange-500 focus:ring-4 focus:ring-orange-500/10';
const LoadingCard = () => <div className="rounded-3xl border border-slate-200 bg-white p-8 text-sm text-slate-500">Cargando sucursales…</div>;
const EmptyCard: React.FC<{ text: string }> = ({ text }) => <div className="rounded-3xl border border-dashed border-slate-300 bg-white p-8 text-sm text-slate-500">{text}</div>;
const Stat: React.FC<{ value: string; label: string }> = ({ value, label }) => <div><p className="text-3xl font-black text-orange-400">{value}</p><p className="mt-1 text-sm text-slate-400">{label}</p></div>;
const statusLabel = (status: string) => ({ pending: 'En preparación', at_branch: 'En sucursal', picked_up: 'Recogido', out_for_delivery: 'En ruta de entrega', delivered: 'Entregado', cancelled: 'Cancelado' } as Record<string, string>)[status] || status;
const formatDate = (value: string) => { try { return new Intl.DateTimeFormat('es-DO', { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(value)); } catch { return value; } };
const money = (value: number, currency = 'DOP') => new Intl.NumberFormat('es-DO', { style: 'currency', currency, maximumFractionDigits: 2 }).format(Number(value || 0));
