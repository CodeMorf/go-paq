import React, { useEffect, useState } from 'react';
import { AlertCircle, Battery, Plus, RefreshCw, Star, Truck, User } from 'lucide-react';
import { ApiClient } from '../../api/client';
import { useApp } from '../../context/AppContext';
import { Button, Card, MetricCard } from '../ui/DesignSystem';

export const DriversFleet: React.FC = () => {
  const { addToast, formatMoney } = useApp();
  const [drivers, setDrivers] = useState<any[]>([]);
  const [activeTab, setActiveTab] = useState<'drivers' | 'vehiculos'>('drivers');
  const [loading, setLoading] = useState(true);
  const [creating, setCreating] = useState(false);
  const [error, setError] = useState('');
  const [branches, setBranches] = useState<any[]>([]);
  const [form, setForm] = useState({ name: '', email: '', phone: '', licenseNumber: '', vehicleType: '', vehiclePlate: '', branchId: '' });

  const load = async () => {
    setLoading(true);
    const result = await ApiClient.getDrivers();
    if (result.success) { setDrivers(result.drivers || []); setError(''); } else setError(result.error);
    setLoading(false);
  };
  useEffect(() => { void load(); }, []);
  useEffect(() => { void ApiClient.getBranches().then((result) => { if (result.success) { setBranches(result.branches || []); setForm((current) => ({ ...current, branchId: current.branchId || result.branches?.[0]?.id || '' })); } }); }, []);
  const vehicles = Array.from(new Map(drivers.filter(driver => driver.vehicle_type || driver.vehicle_plate).map(driver => [`${driver.vehicle_type}-${driver.vehicle_plate}`, driver])).values());
  const create = async (event: React.FormEvent) => {
    event.preventDefault();
    setCreating(true);
    setError('');
    const result = await ApiClient.createDriver({ ...form, email: form.email || undefined });
    setCreating(false);
    if (!result.success) { setError(result.error); return; }
    addToast('success', 'Conductor creado', 'El perfil operativo quedó persistido y puede ser asignado a rutas.');
    setForm({ name: '', email: '', phone: '', licenseNumber: '', vehicleType: '', vehiclePlate: '', branchId: branches[0]?.id || '' });
    await load();
  };

  return <div className="space-y-6"><div className="flex flex-wrap items-center justify-between gap-4"><div><h2 className="text-xl font-extrabold text-slate-900 dark:text-white flex items-center gap-2"><Truck className="w-6 h-6 text-indigo-600" />Conductores y flota</h2><p className="text-xs text-slate-500 dark:text-slate-400 mt-1">Aquí se crean perfiles operativos reales; la cuenta de acceso se enlaza cuando exista un usuario DRIVER de la misma organización.</p></div><Button variant="secondary" size="sm" icon={<RefreshCw className="w-4 h-4" />} onClick={() => void load()}>Actualizar</Button></div>{error && <div className="rounded-xl border border-rose-200 bg-rose-50 p-4 text-sm text-rose-700 flex gap-2"><AlertCircle className="w-4 h-4" />{error}</div>}
    <Card><div className="mb-4 flex items-center gap-2"><Plus className="h-5 w-5 text-indigo-600" /><div><h3 className="text-sm font-bold">Registrar conductor</h3><p className="mt-1 text-xs text-slate-500">El servidor valida la sucursal, licencia y placa antes de guardar.</p></div></div><form onSubmit={create} className="grid gap-4 md:grid-cols-2"><label className="text-xs font-bold">Nombre<input required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} className="mt-1 w-full rounded-xl border border-slate-200 p-3 text-sm dark:border-slate-700 dark:bg-slate-950" /></label><label className="text-xs font-bold">Correo opcional<input type="email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} className="mt-1 w-full rounded-xl border border-slate-200 p-3 text-sm dark:border-slate-700 dark:bg-slate-950" /></label><label className="text-xs font-bold">Teléfono<input required value={form.phone} onChange={(event) => setForm({ ...form, phone: event.target.value })} className="mt-1 w-full rounded-xl border border-slate-200 p-3 text-sm dark:border-slate-700 dark:bg-slate-950" /></label><label className="text-xs font-bold">Licencia<input required value={form.licenseNumber} onChange={(event) => setForm({ ...form, licenseNumber: event.target.value })} className="mt-1 w-full rounded-xl border border-slate-200 p-3 text-sm dark:border-slate-700 dark:bg-slate-950" /></label><label className="text-xs font-bold">Tipo de vehículo<input required value={form.vehicleType} onChange={(event) => setForm({ ...form, vehicleType: event.target.value })} placeholder="Motocicleta, van, camión" className="mt-1 w-full rounded-xl border border-slate-200 p-3 text-sm dark:border-slate-700 dark:bg-slate-950" /></label><label className="text-xs font-bold">Placa<input required value={form.vehiclePlate} onChange={(event) => setForm({ ...form, vehiclePlate: event.target.value })} className="mt-1 w-full rounded-xl border border-slate-200 p-3 text-sm dark:border-slate-700 dark:bg-slate-950" /></label><label className="text-xs font-bold md:col-span-2">Sucursal<select required value={form.branchId} onChange={(event) => setForm({ ...form, branchId: event.target.value })} className="mt-1 w-full rounded-xl border border-slate-200 bg-white p-3 text-sm dark:border-slate-700 dark:bg-slate-950"><option value="" disabled>Selecciona una sucursal</option>{branches.map((branch) => <option key={branch.id} value={branch.id}>{branch.name} · {branch.code}</option>)}</select></label><div className="md:col-span-2"><Button type="submit" variant="primary" disabled={creating || !form.branchId}>{creating ? 'Guardando…' : 'Crear conductor'}</Button></div></form></Card>
    <div className="grid grid-cols-1 sm:grid-cols-3 gap-4"><MetricCard title="Conductores activos" value={String(drivers.length)} subtitle="Registros del servidor" icon={<User className="w-5 h-5" />} accent="indigo" /><MetricCard title="Disponibles" value={String(drivers.filter(d => ['available', 'idle'].includes(d.status)).length)} subtitle="Estado persistido" icon={<Battery className="w-5 h-5" />} accent="emerald" /><MetricCard title="Vehículos identificados" value={String(vehicles.length)} subtitle="Desde asignaciones reales" icon={<Truck className="w-5 h-5" />} accent="purple" /></div><div className="flex gap-2 border-b border-slate-200 dark:border-slate-800 text-xs font-semibold"><button onClick={() => setActiveTab('drivers')} className={`pb-3 px-3 border-b-2 ${activeTab === 'drivers' ? 'border-indigo-600 text-indigo-600' : 'border-transparent text-slate-500'}`}>Conductores ({drivers.length})</button><button onClick={() => setActiveTab('vehiculos')} className={`pb-3 px-3 border-b-2 ${activeTab === 'vehiculos' ? 'border-indigo-600 text-indigo-600' : 'border-transparent text-slate-500'}`}>Vehículos ({vehicles.length})</button></div>{loading ? <Card><p className="text-sm text-slate-500">Cargando flota desde GoPaq…</p></Card> : activeTab === 'drivers' ? <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">{drivers.map(driver => <Card key={driver.id} className="space-y-4"><div className="flex items-start justify-between gap-3"><div className="flex items-center gap-3"><div className="w-11 h-11 rounded-xl bg-indigo-50 text-indigo-600 flex items-center justify-center"><User className="w-5 h-5" /></div><div><h4 className="text-sm font-bold">{driver.name}</h4><p className="text-[11px] text-slate-500">{driver.user_email || driver.email || 'Cuenta no enlazada'}</p></div></div><span className="rounded-full bg-slate-100 px-2 py-1 text-[10px] font-bold">{driver.status || 'sin estado'}</span></div><div className="space-y-1.5 rounded-xl bg-slate-50 dark:bg-slate-800/60 p-3 text-xs"><div className="flex justify-between"><span className="text-slate-400">Vehículo:</span><span className="font-semibold">{driver.vehicle_type || 'No asignado'}</span></div><div className="flex justify-between"><span className="text-slate-400">Placa:</span><span className="font-mono font-bold">{driver.vehicle_plate || '—'}</span></div><div className="flex justify-between"><span className="text-slate-400">Posición:</span><span className="font-mono">{driver.current_lat != null && driver.current_lng != null ? `${Number(driver.current_lat).toFixed(5)}, ${Number(driver.current_lng).toFixed(5)}` : 'Sin GPS'}</span></div><div className="flex justify-between"><span className="text-slate-400">COD:</span><span className="font-mono">{formatMoney(Number(driver.cod_collected_today || 0))}</span></div></div><div className="flex items-center gap-1 text-xs text-amber-500"><Star className="w-3.5 h-3.5 fill-amber-500" />{driver.rating ?? 'Sin calificación'}</div></Card>)}</div> : <Card><h3 className="text-sm font-bold mb-4">Vehículos asociados a conductores</h3>{!vehicles.length ? <p className="text-sm text-slate-500">No hay vehículos registrados en este tenant.</p> : <div className="grid grid-cols-1 md:grid-cols-3 gap-4">{vehicles.map(driver => <div key={driver.id} className="rounded-xl border border-slate-200 dark:border-slate-700 p-4"><p className="font-bold">{driver.vehicle_type || 'Vehículo'}</p><p className="mt-1 font-mono text-sm text-indigo-600">{driver.vehicle_plate || 'Sin placa'}</p><p className="mt-3 text-xs text-slate-500">Conductor: {driver.name}</p></div>)}</div>}</Card>}</div>;
};
